<UserControl x:Class="zdzhantai.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="380">
    <Grid Height="300" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="17*"/>
            <ColumnDefinition Width="262*"/>
            <ColumnDefinition Width="21*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="14*"/>
            <RowDefinition Height="253*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="23*"/>
        </Grid.RowDefinitions>
        <MediaElement  Name="media" Grid.Row="1"  Grid.Column="1"   LoadedBehavior="Manual"  MouseLeftButtonUp="MediaPlayer_MouseLeftButtonUp"/>
        <TextBlock     Name="text"  Grid.Row="3"  Grid.Column="1"   Text="自制控件" />
    </Grid>
</UserControl>
